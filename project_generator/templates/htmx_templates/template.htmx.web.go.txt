package web

import "embed"

// Files holds the templates and static assets. Embedding them means the built
// binary is the only file you have to deploy.
//
//go:embed templates/*.html static/*
var Files embed.FS
